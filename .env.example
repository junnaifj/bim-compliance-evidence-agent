# Optional operator-managed OpenAI credential. Keep this server-side and never
# rename it with a NEXT_PUBLIC_ prefix.
OPENAI_API_KEY=

# The current stable alias is recommended so the deployed app receives model
# improvements without a code change. Users may choose another exposed model.
OPENAI_MODEL=gpt-5.6
